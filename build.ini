[logging]
level = INFO
format = %%(asctime)s - %%(levelname)s: %%(message)s

[project]
name = Sconsolidator
prefix = ch.hsr.ifs.sconsolidator

[mappings]
root = root
features = features
helps = helps
bundles = bundles
releng = releng
tests = tests

[build]
target = Target
